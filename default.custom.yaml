# default.custom.yaml
# save it to:
#   ~/.config/ibus/rime  (linux)
#   ~/Library/Rime       (macos)
#   %APPDATA%\Rime       (windows)

patch:
  schema_list:
    - schema: flypy_xhfast
    - schema: flypy_radical
    - schema: melt_eng
    - schema: flypy_phrase
    # - schema: flypy_zrmfast
    # - schema: emoji               # emoji表情
  ascii_composer:
    switch_key:
      Shift_L: commit_code
      Shift_R: commit_code